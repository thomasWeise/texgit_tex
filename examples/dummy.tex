This is a dummy text file.
It just contains this text, nothing else.
It can directly be included into \LaTeX.
Since we directly \verb=\input= it, it can also contain macros.
And math: $1+2=\sqrt{9}$.
